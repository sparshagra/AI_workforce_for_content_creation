from src.orchestrator.agent_controller import run_pipeline

if __name__ == "__main__":
    user_prompt = "We are Ufresh, a dairy brand. Create a Diwali-themed ad campaign post."
    run_pipeline(user_prompt)
